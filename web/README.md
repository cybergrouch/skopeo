# Skopeo Web

The Skopeo web client — sign-up / sign-in plus a **capability-gated dashboard**
(profile, standings, event organizing, ratings, admin, and more) and public
share pages. React + TypeScript (Vite), Tailwind + shadcn/ui, Firebase Auth, and
a typed TanStack Query client generated from the backend's OpenAPI spec with
[orval](https://orval.dev). See
[WEB_UI_ARCHITECTURE.md](../docs/engineering/architecture/WEB_UI_ARCHITECTURE.md)
for the full architecture.

## Stack

| Concern        | Choice                                                          |
| -------------- | -------------------------------------------------------------- |
| Build / dev    | Vite                                                           |
| UI             | Tailwind CSS v4 + shadcn/ui (components in `src/components/ui`) |
| Routing        | React Router                                                   |
| Data fetching  | TanStack Query, hooks generated by orval                       |
| Auth           | Firebase Auth (email/password + Google)                        |
| Hosting target | Firebase Hosting                                               |

## Prerequisites

- Node 22+ and npm
- A Firebase project with **Authentication** enabled (Email/Password + Google)
- The Skopeo backend running (defaults to `http://localhost:8080`)

## Setup

```bash
cd web
npm install
cp .env.example .env.local   # fill in your Firebase web app config
npm run dev                  # http://localhost:5173
```

`.env.local` holds the **public** Firebase client config (safe in the browser);
the Firebase secret stays server-side in Firebase. In dev, `/api` is proxied to
`http://localhost:8080`, so the browser talks to a single origin and the Firebase
ID token rides along to the backend, which verifies it against Firebase's JWKS.

## Scripts

| Command                | Description                                                |
| ---------------------- | ---------------------------------------------------------- |
| `npm run dev`          | Dev server (regenerates the API client first)             |
| `npm run build`        | Type-check + production build (regenerates the API client) |
| `npm run typecheck`    | `tsc` only (regenerates the API client)                   |
| `npm run lint`         | ESLint                                                     |
| `npm run api:generate` | Regenerate the typed API client from the OpenAPI spec     |

## Generated API client

`npm run api:generate` (also run automatically before `dev`, `build`, and
`typecheck`) reads `../src/main/resources/openapi/documentation.yaml` — the
backend's hand-maintained, test-verified spec — and emits typed TanStack Query
hooks into `src/api/generated/` (gitignored). All requests go through the axios
mutator in `src/api/axios.ts`, which attaches the current Firebase ID token.

Config lives in `orval.config.ts`. Because the spec is the single source of
truth, a backend contract change surfaces here as a type error.

## Sign-up flow

1. The user authenticates with Firebase (email/password or Google) → gets an ID token.
2. The client calls `POST /api/v1/users` (idempotent) to provision the Skopeo
   profile. Identity/email come from the verified token, not the request body.
3. New users complete their profile at **`/complete-profile`** (required fields),
   then land on **`/dashboard`**. Until an administrator assigns an initial rating,
   the account is *pending assessment* and ineligible for match fixtures.

## Routes

Public / unauthenticated:
- `/signup`, `/login` — create account / sign in
- `/about` — product overview
- `/invite` — accept an admin invite (email-link onboarding)
- `/players/:code`, `/matches/:code`, `/events/:code`, `/clubs/:code` — public share pages (no login), with QR

Authenticated:
- `/complete-profile` — finish the required profile (`RequireAuth`)
- `/dashboard` — the capability-gated dashboard; the active tab is synced to `?tab=` (`RequireAuth` + `RequireProfile`)

See the dashboard tab table in
[WEB_UI_ARCHITECTURE.md](../docs/engineering/architecture/WEB_UI_ARCHITECTURE.md#dashboard-structure).

## Deployment (Firebase Hosting)

Hosting config lives in the repo-root `firebase.json` (serves `web/dist`, SPA
rewrites to `index.html`, immutable caching for hashed assets). The
`.github/workflows/deploy-web.yml` workflow builds and deploys to the **live**
channel. It is **release-driven** — dispatched by the tag-and-ship release flow
for a release tag (in lockstep with the API), or run manually via
`workflow_dispatch` — **not** on every push to `main`.

The deploy job is **skipped until configured** (guarded on the
`VITE_FIREBASE_PROJECT_ID` repo variable), so the workflow is inert until you add:

**Repository → Settings → Secrets and variables → Actions**

- **Variables** (public Firebase client config, inlined into the bundle):
  `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`,
  `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_APP_ID`, and `VITE_API_BASE_URL`
  (the deployed API origin, e.g. `https://api.skopeo.app`).
- **Secret**: `FIREBASE_SERVICE_ACCOUNT` — JSON for a service account with the
  *Firebase Hosting Admin* role (Firebase console → Project settings → Service
  accounts → Generate new private key). This is the only secret; the Firebase
  app secret stays in Firebase.

### Manual deploy

```bash
cd web && npm run build      # produces web/dist
cd .. && firebase deploy --only hosting   # uses firebase.json; requires firebase-tools + login
```
